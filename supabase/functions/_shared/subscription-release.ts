/** Release switch shared by the app and checkout endpoint.
 * Keep false for the fully free release. See docs/subscriptions.md before
 * re-enabling; billing designs, products and reconciliation remain intact. */
export const SUBSCRIPTIONS_ENABLED: boolean = false;
