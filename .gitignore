node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Apple "Sign in with Apple" private key — never commit this.
*.p8

# `npx cap sync ios` output. Regenerated from dist/ on every build, and
# dist/ is ignored above — ios/App/App/public is a byte-for-byte copy of it.
# Only the hand-written Swift and the privacy manifest under ios/ are tracked.
ios/App/App/public/
ios/App/App/capacitor.config.json
ios/App/App/config.xml
ios/capacitor-cordova-ios-plugins/
ios/App/Pods/
ios/App/build/
ios/App/output/
ios/DerivedData/
xcuserdata/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/app.css

# Generated by scripts/generate-icons.mjs, staged for the (untracked) Xcode project.
ios-assets/

# Supabase CLI cache
supabase/.temp/
